name: C/C++ CI

on:
  push:
    branches:
    - main
    - develop
    
  pull_request:
    branches:
    - main
    - develop

jobs:
  build:
    runs-on: ubuntu-latest
    name: unit testing
    steps:
    - uses: actions/checkout@v3
    - name: unit testing
      run: make tests
